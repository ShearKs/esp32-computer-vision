<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Permitir tráfico cleartext (HTTP) a CUALQUIER IP de red privada.
         Así no importa si la IP del backend cambia. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Redes privadas habituales: 192.168.x.x, 10.x.x.x, 172.16-31.x.x -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Cualquier 192.168.x.x -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">192.168.48.0</domain>
        <!-- Rangos genéricos para futuras redes -->
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
